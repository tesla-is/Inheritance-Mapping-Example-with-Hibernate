CREATE TABLE  "EMP1001" 
   (	"ID" NUMBER(10,0), 
	"TYPE" VARCHAR2(30), 
	"NAME" VARCHAR2(30), 
	"SALARY" FLOAT(126), 
	"BONUS" NUMBER(10,0), 
	"PAY_PER_HOUR" FLOAT(126), 
	"CONTRACT_DURATION" VARCHAR2(50)
   )
/
